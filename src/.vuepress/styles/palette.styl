// 颜色
$accentColor = #333;
$textColor = #333;
$borderColor = #eaecef;
$codeBgColor = #282c34;
$arrowBgColor = #2c3e50;
$badgeTipColor = #DA5961;
$badgeWarningColor = darken(#ffe564, 35%);
$badgeErrorColor = #DA5961;

// 布局
$navbarHeight = 3.6rem;
$sidebarWidth = 20rem;
$contentWidth = 1000px;
$homePageWidth = 960px;

// 响应式变化点
$MQNarrow = 959px;
$MQMobile = 719px;
$MQMobileNarrow = 419px;

.blog-tags {
    width: 100%;
}

@media (min-width: 1200px) {
    .vuepress-toc {
        display: block;
    }
}